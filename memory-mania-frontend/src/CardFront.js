import React from 'react';

const CardFront = props => {

    return (
      //<div className="card-front" style={{backgroundImage: `url(${props.imageUrl})`}}>
      <div className="card-front" style={{backgroundColor: "#000000"}}>
      </div>
    )
  }

export default CardFront